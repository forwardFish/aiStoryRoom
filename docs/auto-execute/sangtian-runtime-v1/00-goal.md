# 桑田诏 Runtime MVP 并行交付目标

- 项目：`D:\\tmp\\aiStoryRoom-sangtian-parallel-dev-20260811-190048`
- 分支：`codex/chatgpt-pro-sangtian-runtime-v1`
- 基线：`99031a083310f113457e210cf5a391e680d0a5d2`
- 目标：实现 P0—N7 可玩的单人 MVP，使自由输入经过合法行动编译后，按“行动兑现 → 时间推进 → 五席/NPC 反应 → 压力升级 → 新现场 → 节点冻结 → Finale”持续推进。

## 成功标准

1. D2 权威运行时内核消除已记录的幂等、权限、持久化、冻结与恢复阻断缺陷。
2. D3 生成受事实与知识边界约束的四节拍叙事，并完成五个 AI 席位、条件应变和 Finale 编排。
3. D4 在现有 `/game` 三栏中完成建议输入、自由输入、可见后果、1 人 + 5 AI 状态，以及“我先睡一下”的真实浏览器闭环。
4. 精确远程 SHA 在干净验证环境中通过适用的内容、合同、类型、单元、集成、Web、构建、恢复和浏览器门禁。

## 明确不做

- 不重做视觉系统，不新建第二个游戏页面。
- 不引入第二事实源、故事专用正则或客户端可写的内部 `effect/statePatch`。
- 不合并 `main`、不建 PR、不部署、不执行生产迁移、不操作真实用户数据。

## 停止条件

仅在登录/凭据、人机验证、生产权限、不可逆操作，或同一阻断缺陷连续五轮仍无法安全修复时停止。

最终判定：`REPAIR_REQUIRED`（实现中）。
