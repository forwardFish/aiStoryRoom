# AI 代理项目规则

本规则适用于在本仓库中工作的所有 Codex 任务、代理、子代理、自动化流程、
外部 AI 协作者及人工辅助流程。项目所有者在当前对话中的明确指令拥有最高优先级。

## 工作树基线与收尾规则（强制）

1. **新任务开始前工作树必须干净。**
   - 开始任何写操作前，必须确认当前分支为 `main`，执行 `git fetch origin main`，并确认
     本地 `HEAD`、`origin/main` 与实时远程 `main` SHA 一致。
   - `git status --porcelain` 必须为空，暂存区不得包含上一任务内容。若不为空，必须先由
     原任务完成范围化提交和推送，或经项目所有者批准后备份并移出工作树；不得把遗留内容
     带入新任务。

2. **同一时间只允许一个任务写 `main`。**
   - 写任务开始前，必须在已忽略目录 `.codex-runtime/` 中创建
     `main-writer.lock`，记录任务名称、开始时间和基线 SHA。
   - 如果锁已存在，其他任务只能进行只读检查，不得编辑、暂存、提交或推送。
   - 只有持锁任务完成收尾并确认工作树干净后才能删除该锁；无充分证据不得擅自清除锁。

3. **每个模块完成后必须立即范围化提交并推送。**
   - 模块通过聚焦测试和对应验收门后，必须只暂存该模块的准确文件，检查 staged diff，
     随即提交到本地 `main` 并推送到远程 `main`。
   - 不得用 `git add .`、不得把多个未验收模块积压成一次大提交、不得把未提交模块跨入
     下一个任务。
   - Git 提交不等于部署；部署、数据库迁移、生产配置和真实用户数据操作仍需单独授权。

4. **任务完成必须以远程回读和干净工作树收尾。**
   - 必须使用 `git ls-remote` 回读远程 `main`，确认本地、tracking 和实时远程 SHA 完全一致，
     Ahead/Behind 为 `0/0`。
   - 必须确认暂存区为空且 `git status --porcelain` 为空；只要还有遗留文件，就不得声明任务
     已完全收尾。
   - 最终报告必须给出提交 SHA、推送目标、验证结果和剩余工作；没有剩余工作时明确报告
     `CLEAN=true`。

5. **本地运行产物不得污染工作树。**
   - `.auth-mail-sink.ndjson`、`.codex-runtime/`、`apps/openovel-runtime/dist-cjs/` 等本地邮件、
     任务锁和生成产物必须由 `.gitignore` 排除，不得暂存、提交或用于证明产品验收。
   - 如需清理既有混合工作树，必须先在仓库外保存修改清单、二进制补丁和未跟踪文件副本，
     再建立包含未跟踪文件的具名 stash；两份恢复证据验证成功后才能执行 reset 或 clean。

## 分支规则（强制）

1. **所有正常开发都直接在 `main` 分支进行。**
   - 修改文件前必须确认当前分支是 `main`。
   - 不得以常规预防措施为由创建 feature、fix、task、temporary、`codex/*`
     或其他开发分支。
   - 不得通过 detached HEAD 或额外开发 worktree 绕过本规则。

2. **创建其他开发分支必须事先获得项目所有者批准。**
   - 如果冲突、并发任务、权限、工具或工作树状态导致无法安全地在 `main`
     开发，必须在创建或切换分支前停止操作。
   - 必须立即说明冲突原因、受影响工作、风险和拟创建的准确分支或
     worktree 名称。
   - 必须等待项目所有者明确批准。一次批准只适用于当次说明的分支和场景，
     不得视为长期授权。
   - 禁止先创建分支，再补问批准。

3. **并发任务冲突必须公开，禁止隐藏处理。**
   - 始终假设可能有多个 Codex 任务共享当前工作区。
   - 编辑前检查当前分支和工作树状态。
   - 保留其他任务的修改，不得覆盖、重置、丢弃或擅自吸收。
   - 如果修改范围重叠或无法确认归属，暂停冲突工作并通知项目所有者。

4. **`release` 只用于部署。**
   - 不得在 `release` 上进行功能开发或普通修复。
   - `main` 是开发代码的唯一事实来源。
   - 只有经过审查和验证、并获得上线授权的 `main` 提交才能进入 `release`。
   - 不得把 `release` 当作解决冲突或临时开发的分支。

5. **验证克隆不是开发分支。**
   - 可以创建一次性的干净克隆，用于验证某个准确的 `main` 提交。
   - 不得在验证克隆中编写或提交修复。需要修复时必须回到 `main`；如果
     `main` 无法安全开发，则执行前述审批流程。

## 玩家可见页面审批规则（强制）

本节适用于现在和未来的所有功能，也适用于所有代理、子代理、外部
ChatGPT、Claude、Gemini 协作者、自动化及人工辅助流程。

1. **所有正式玩家可见改动都必须事先取得项目所有者明确批准。**
   - 未经批准，不得编辑、生成、替换、重做样式、改名、改路由或删除任何
     玩家页面、组件、布局、交互、文案、图片、图标或动画。
   - 批准只覆盖明确说明的页面、路由、文件和可见结果。其他页面或过去的
     批准不能自动沿用。
   - 测试通过、设计文档、代理建议或后端需求都不能代替项目所有者批准。

2. **页面冻结范围包含间接可见变化。**
   - 如果 API 投影、公开目录数据、角色名称、头像、公开简介、路由选择、
     功能开关或响应结构会改变玩家看到的内容或进入的页面，它们同样属于
     页面改动。
   - 未经批准，不得暂存、提交、推送、部署或启用这些直接或间接页面改动。

3. **新增功能必须保留已经批准的正式页面。**
   - 优先在现有页面背后实现领域逻辑、权威链、持久化和面向玩家的安全数据
     合同。
   - 新后端数据必须适配已有页面和已有交互，不得为了方便而在真实产品流程
     中创建平行游戏页面、替代渲染器、临时玩家外壳或测试专用产品路由。
   - 后端内部代码、哈希、证据 ID、控制权 fence、Provider 信息和调试标签
     不得直接显示给玩家。

4. **临时测试页面只能存在于隔离测试边界。**
   - 为开发和验证可以创建临时测试页面或测试路由，但不得改变已经批准的
     正式页面。
   - 临时测试页面不得被真实玩家流程、生产路由、入口、导航、公开目录、
     功能开关或运行时回退逻辑链接、跳转、选择、导入或启用。
   - 必须明确标记为“仅供测试”，并隔离测试数据、认证、存储和 URL。
   - 测试结束后必须立即删除临时页面和路径，删除全部引用，并验证正式流程
     中不存在残留路由、导入、重定向、开关、资源或导航入口。
   - 除非项目所有者明确批准保留某个具名测试工具，否则临时测试页面和路径
     不得暂存、提交、推送或部署。测试完成不代表允许保留页面。

5. **修改正式页面前必须提交审批说明。**
   - 审批说明必须包含：修改原因、影响的路由和用户流程、准确文件、修改前后
     行为、数据合同影响、视觉参考或示意图、测试方案和回滚方案。
   - 必须等待项目所有者明确批准后才能开始修改。
   - 如果实施范围需要超出已批准内容，必须停止并重新申请批准。

6. **桑田 `/game` 页面有冻结的事实来源。**
   - 权威规范和六张视觉参考位于 `docs/主游戏最终版/`。
   - 必须保留真实 `/game` 页面及已批准的三栏信息架构，不得新增平行主游戏
     页面。
   - 视觉和交互验收必须使用真实 `/game` 路由与六张参考图进行对比；只有
     合同测试或静态模拟页面不能证明页面验收通过。

7. **意外页面变化是立即停止条件。**
   - 如果后端或集成修改意外改变页面，立即停止页面工作，不得发布受影响文件，
     并向项目所有者报告准确原因和受影响文件。
   - 不得自行“优化”、重新设计或直接修补正式页面。必须先提交范围明确的恢复
     方案，并等待批准。

8. **提交和推送前必须证明没有越过页面边界。**
   - 每次提交或推送前，必须列出所有已暂存的玩家可见文件，以及每个文件对应
     的批准内容。
   - 如果没有页面修改批准，已暂存的玩家可见文件列表必须为空。

## 高内聚、低耦合与模块化开发规则（强制）

本节适用于现在和未来的全部开发、修复、重构、性能优化、测试和验收工作。
详细标准见 `docs/standards/高内聚低耦合与模块化开发标准_v1.0.md`。

1. **代码模块化，不得只把开发计划分阶段。**
   - 每个模块必须只承担一个可以独立描述的职责，并明确输入、输出、依赖、
     权威来源、失败方式和测试边界。
   - 同一个模块不得同时负责内容读取、规则裁定、持久化、模型表达和页面渲染
     等多个独立职责。

2. **开发前必须先提交模块清单。**
   - 模块清单必须包含：模块名称、职责、非职责、准确文件、输入、输出、依赖、
     测试、回滚方式、玩家参与节点和问题归属。
   - 未完成模块清单和依赖方向说明，不得开始业务代码修改。

3. **模块必须高内聚。**
   - 一个需求的同类规则必须集中在唯一模块，不得散落在页面、API、数据库适配器
     和 Prompt 中分别实现。
   - 同一事实或规则只能有一个权威来源；其他模块只能读取或投影，不得复制并形成
     第二权威。

4. **模块之间必须低耦合。**
   - 模块只能通过窄而明确的合同通信，不得读取另一个模块的内部表、私有变量、
     临时缓存或未经批准的原始载荷。
   - 依赖必须单向。领域规则不得依赖 API、页面、Provider 或数据库适配细节；
     Narrator 不得修改 Settlement；页面不得重新实现领域规则。

5. **每个模块必须能够独立诊断。**
   - 每个模块必须有聚焦测试和明确的错误归属；失败证据必须能说明问题发生在输入、
     编译、裁定、持久化、生成、校验、投影或渲染中的哪一层。
   - 后端内部错误码、证据 ID 和诊断标签不得直接显示给玩家。

6. **每次只允许一个模块进入实现与验收。**
   - 当前模块通过聚焦测试和对应验收门后，才能进入下一个模块。
   - 玩家可见模块完成后必须立即提供真实页面供项目所有者参与测试；未经认可，
     不得以继续开发后续模块来掩盖当前模块问题。

7. **跨模块修改必须停止并重新说明。**
   - 如果一个修复需要越过原模块边界、修改公共合同、数据库、路由、权威链或三个
     以上既有模块，必须先停止，说明根因、耦合风险、准确扩展范围和替代方案。
   - 不得用通用 Manager、万能 Service、共享可变状态、重复规则或新增平行运行时
     快速绕过模块边界。

8. **完成声明必须按模块提供证据。**
   - 必须分别列出每个模块修改的文件、行为变化、未改范围、测试结果和已知问题。
   - 一个模块测试通过不等于整个需求通过；只有全部批准模块及真实用户流程完成验收，
     才能声明整体完成。

## 并行开发与串行集成规则（强制）

1. **剧情决策主链禁止并行写入。**
   - Story Package、Decision/Catalog、Working Ledger、Settlement、Narrative Context、
     `/game` Projection、决策提交路径和 Web 展示属于同一因果链。
   - 同一时间只能有一个任务修改该链；其他任务只能只读分析、准备方案，或处理合同
     已冻结且完全不重叠的模块。

2. **修改“剧情 + 决策”流程必须事先确认。**
   - 任何会改变剧情来源、决策产生方式、Beat 推进、玩家行动提交、自动席位行动、
     Settlement、结果剧情、下一决策、Narrative 投影或 `/game` 展示顺序的修改，
     实施前必须取得项目所有者在当前任务中的明确确认。
   - 如果尚未取得逐项确认，必须先在开发文档中写清修改原因、修改前后流程、准确
     文件、权威来源、影响范围、测试方案和回滚方式，并等待项目所有者确认该文档后
     才能开发。
   - AI、子代理、外部协作者、测试结果或开发文档本身均不能代替项目所有者授权。
     实施中如实际范围超出已确认内容，必须立即停止并重新确认。

3. **任务必须绑定准确基线和模块边界。**
   - 开发前必须记录基线 SHA、唯一模块、准确文件、权威输入、输出、非职责、依赖
     任务、禁止假设、聚焦测试和合并后的真实流程。
   - 没有上述信息不得开始业务代码修改。

4. **AI 不得自行补全未定义行为。**
   - 当前代码、权威文档和任务卡没有明确说明的行为一律视为未知。
   - 不得根据常见做法自行新增状态、路由、公共合同、数据库表、回退逻辑、权威来源
     或产品流程；需要越过任务边界时必须停止并报告。

5. **候选代码合并前必须重新审查当前 `main`。**
   - 必须比较任务基线之后的依赖文件、公共合同和权威链变化。
   - 任一依赖或权威输入已经变化时，候选代码视为过期，必须重新进行语义审查；不得
     仅解决 Git 冲突后机械合并。

6. **一次只能集成一个模块。**
   - 每个模块完成后必须单独测试、提交、推送并运行跨模块黄金流程。
   - 当前模块未通过前不得合并下一个模块；禁止多个功能集中合并后再统一排错。

7. **每次集成必须验证同一条真实黄金流程。**
   - 至少验证：新 Solo Run、正确开场、剧情自然推出决策、行动只提交一次、正确推进
     当前 Beat、结果剧情承接真实选择、下一决策保持开放、刷新后仍处于正确位置。
   - 如果当前模块没有修改正式玩家页面，黄金流程验证不得被解释为页面改版授权。

8. **性能路径不得先于语义稳定。**
   - SQL7、FAST、缓存和内存投影等优化必须建立在已通过的普通权威路径之上，并证明
     语义等价。
   - 不得把单个 N1 决策的假设扩展为整个章节或 N2–N7 的通用行为。

## 网页版 ChatGPT Pro 的 Chat 模式协作规则（强制）

本节只适用于项目所有者明确要求使用网页版 ChatGPT Pro 协作的任务。ChatGPT Pro
是外部高级工程师，负责深入研究、方案设计和编写主要代码；Codex 是总负责人，负责
理解需求、检查仓库、准备脱敏源码、分配任务、监控进度、追问纠错、机械落地代码并
独立验收。ChatGPT Pro 的自述、报告、测试结果或附件不得直接视为验收通过。

1. **只能使用网页版 ChatGPT Pro 的普通 Chat 模式。**
   - 禁止把本节所称的 ChatGPT Pro 替换为 Codex 子代理、本地模型、API 模型、
     Deep Research、Work 模式、Agent 模式或其他后台代理。
   - 必须保存并报告实际使用的 ChatGPT Pro 对话链接。
   - 多个相互独立的复杂任务必须使用相互独立的 Pro 对话，避免上下文和源码范围污染。

2. **开始协作前必须核对仓库和约束。**
   - Codex 必须先阅读仓库中的 `AGENTS.md`、存在的 `CLAUDE.md`、`README`、
     `package.json` 及与任务直接相关的架构和验收文档。
   - 必须检查当前分支、`HEAD`、远程基线和工作树状态，不得覆盖、重置、丢弃或
     擅自吸收现有修改。
   - 必须先明确需求、不可破坏边界、模块清单、交付物、测试和验收标准，再允许
     Pro 编写代码。

3. **Pro 开发分支必须事先获得本次明确授权。**
   - 当项目所有者在当前任务中明确要求“基于 `main` 创建新分支供 Pro 开发”时，
     才允许创建该任务专用分支；该授权优先于本文件的常规 `main` 开发规则，但只
     适用于本次说明的准确分支和任务。
   - 创建前必须确认基线来自最新、准确的 `main`，并报告拟创建的准确分支名称。
   - Pro 相关提交只能进入该专用分支，不得直接修改 `main` 或 `release`。
   - 未获得上述明确授权时，仍执行本文件“分支规则（强制）”，不得自行创建分支。

4. **必须向 Pro 提供完整而脱敏的源码输入。**
   - 不得假设 Pro 能访问本地文件、私有仓库、内部网络或运行环境。
   - 源码 ZIP 默认只包含任务所需源码和必要配置，必须排除 `.git`、`node_modules`、
     构建产物、缓存、数据库、运行状态、浏览器状态及无关的大型文件。
   - ZIP 中不得包含 `.env`、API Key、Token、私钥、Cookie、密码、验证码、连接串
     或其他凭据；上传前必须执行密钥扫描。
   - 必须记录并报告源码基线 commit、ZIP 文件名、大小和 SHA-256。

5. **发给 Pro 的任务书必须可以独立开发和验收。**
   - 任务书至少包含：背景和目标、当前架构、单一权威来源、禁止破坏的边界、准确
     修改范围、非职责、明确交付物、必须执行的测试、禁止操作、禁止声称的结果、
     验收标准和回滚方式。
   - 必须要求 Pro 实际阅读提供的源码并产出真实代码工件；只给方案、伪代码、口头
     说明或不可下载附件不算交付。
   - 未真实运行的测试必须标记为 `TESTS_NOT_RUN`；模拟、静态检查或 CI 不能冒充
     真实 Supabase、浏览器、生产或玩家验收。

6. **长时间运行不得被无故打断。**
   - Pro 可能需要较长时间。不得因为等待时间长就催促、取消、重复发送同一任务或
     新开重复对话。
   - 只有经过合理等待、连续检查确认没有进展，或 Pro 明确请求补充信息时，才检查
     页面状态、重新打开原对话或要求从最后完成位置继续。
   - 页面刷新、上下文截断或连接中断时，Codex 必须使用保存的对话链接自主恢复，
     不得让项目所有者承担普通技术传话工作。

7. **登录和身份验证必须由项目所有者亲自处理。**
   - 遇到登录失效、账号选择、验证码、密码、Passkey、两步验证或 CAPTCHA，必须
     暂停并通知项目所有者。
   - 不得向项目所有者索取密码、Cookie、验证码或恢复码，也不得尝试绕过验证。

8. **Pro 交付后必须由 Codex 独立验收。**
   - 检查报告、补丁、完整源码、manifest 和附件是否齐全，并重新计算文件大小和
     SHA-256。
   - 在隔离的准确基线工作树中先执行补丁检查和审查，确认修改范围、依赖、锁文件、
     可执行流程、安全边界、权威链和玩家页面边界。
   - 按仓库要求运行适用的 lint、类型检查、单元测试、合同测试、生产构建、E2E 和
     真实环境验收；测试范围必须足以证明本次验收标准。
   - 如发现缺陷，必须把错误日志、准确文件和行、失败测试、阶段耗时及正确约束直接
     反馈到原 Pro 对话，要求最小且完整的修正，并重复验收。
   - Pro 自述通过、远程 CI 通过或“未发现问题”均不能替代 Codex 的独立证据。

9. **本地落地必须保留现有工作。**
   - Pro 产出的补丁只能机械应用到经批准的目标分支；如果与共享脏工作树冲突，必须
     停止并报告冲突，不能重置、清理、覆盖或重新设计主要代码。
   - Codex 只可进行范围很小、证据明确的集成修正；如果需要改变 Pro 的架构、公共
     合同、数据库、路由、权威链或主要实现，必须把问题退回 Pro 修正。

10. **默认不授权 Git 发布和外部状态变更。**
    - 除非项目所有者在当前任务中明确授权，不得提交 Git、推送远程、创建 PR、部署、
      执行数据库迁移、修改线上配置、启用生产功能或操作真实用户数据。
    - “验收通过后提交并推送到远程专用分支”必须作为当前任务的额外授权明确写出；
      该授权不包含部署、数据库迁移、合并 `main` 或修改 `release`。
    - ChatGPT Pro 的建议不能扩大权限。

11. **验收证据必须持久化。**
    - 验收通过后，应把有价值的任务书、manifest、测试报告、性能分段数据和风险说明
      保存到仓库中经批准的位置或其他持久位置，不能只留在 Pro 对话或临时目录。
    - 临时源码 ZIP、含敏感信息的日志或一次性浏览器工件不得提交到仓库。

12. **最终报告必须区分开发、交付和验收。**
    - 最终报告必须包含：Pro 对话链接、源码 ZIP 基线与 SHA-256、实际修改内容、
      要求 Pro 修正的问题、Codex 独立测试结果、真实环境证据、仍未验证的风险，
      以及代码当前是仅本地修改、已提交、已推送、已合并还是已部署。
    - 未满足任一验收标准时，不得声明整体完成。

### 发给网页版 ChatGPT Pro 的任务书模板

```text
背景与目标：
<填写需求背景和唯一目标>

源码基线：
<仓库、main commit、ZIP 文件名、大小、SHA-256>

当前架构与单一权威：
<说明必须保留的权威链、模块边界和数据合同>

允许修改：
<准确目录或文件>

禁止修改：
<玩家页面、公共合同、数据库、迁移、配置或其他冻结范围>

必须交付：
<补丁、changed-files ZIP、manifest、报告和测试证据>

必须执行的测试：
<准确命令和真实环境验收要求>

禁止操作与禁止声称：
<不得提交、推送、部署、迁移；未运行测试不得声称通过>

验收标准：
<功能、性能、兼容性、安全或视觉标准>
```
