name: Export source for pressure player-first implementation

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout exact main commit
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.9
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Install exact workspace dependencies
        run: |
          set -euo pipefail
          CI=true pnpm install --frozen-lockfile
          pnpm db:generate
      - name: Archive exact source and dependency tree
        run: |
          set -euo pipefail
          tar --zstd -cf /tmp/aiStoryRoom-source.tar.zst \
            --exclude=.git \
            --exclude=node_modules \
            --exclude='*/node_modules' \
            .
          tar --zstd -cf /tmp/aiStoryRoom-node-modules.tar.zst \
            node_modules \
            apps/api/node_modules \
            apps/openovel-runtime/node_modules \
            packages/shared/node_modules \
            packages/templates/node_modules
          git rev-parse HEAD > /tmp/deps-head.txt
          git rev-parse HEAD^{tree} > /tmp/deps-tree.txt
      - name: Upload exact source and dependencies
        uses: actions/upload-artifact@v4
        with:
          name: aiStoryRoom-main-source-and-dependencies
          path: |
            /tmp/aiStoryRoom-source.tar.zst
            /tmp/aiStoryRoom-node-modules.tar.zst
            /tmp/deps-head.txt
            /tmp/deps-tree.txt
          if-no-files-found: error
          retention-days: 1
