name: Requirement Dependency Evidenced Delivery

on:
  push:
    branches:
      - codex/chatgpt-pro-dynamic-kernel-lite
    paths:
      - .github/workflows/requirement-dependency-evidenced-delivery.yml
  workflow_dispatch:

permissions:
  contents: write
  statuses: write

jobs:
  deliver:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    env:
      EVIDENCE_ROOT: /tmp/requirement-dependency-evidenced
      BASE_SHA: 321a9aa03ce33201a59da63350e385b885023d77
      TARGET_BRANCH: codex/chatgpt-pro-dynamic-kernel-lite
      TRACKED_EVIDENCE: docs/auto-execute/evidence/requirement-dependency/deterministic-summary.json
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 10.12.1
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Install
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "${EVIDENCE_ROOT}/product/logs" "${EVIDENCE_ROOT}/final/logs"
          git rev-parse HEAD > "${EVIDENCE_ROOT}/input-sha.txt"
          pnpm install --frozen-lockfile 2>&1 | tee "${EVIDENCE_ROOT}/product/logs/00-install.log"
      - name: Create clean product commit
        shell: bash
        run: |
          set -euo pipefail
          if [ ! -f packages/templates/src/story-package/requirement-dependency.ts ]; then
            script="$(git ls-files '.github/repair/requirement-dependency-product.py' | head -n1)"
            test -n "$script"
            python "$script" 2>&1 | tee "${EVIDENCE_ROOT}/product/logs/01-apply-product.log"
          else
            echo 'tracked product already materialized' | tee "${EVIDENCE_ROOT}/product/logs/01-apply-product.log"
          fi
          SANGTIAN_SKIP_SOURCE_WRITES=1 \
          SANGTIAN_AUTHORING_OUTPUT_ROOT="${RUNNER_TEMP}/sangtian-authoring-output" \
          SANGTIAN_RUNTIME_PACKAGE_PATH="${GITHUB_WORKSPACE}/packages/templates/config/sangtian/story-package/part-one-runtime.json" \
            node scripts/story-decomposition/compile-sangtian-part-one-authoring.mjs \
            2>&1 | tee "${EVIDENCE_ROOT}/product/logs/02-compile-package.log"
          git diff --check
          git diff --binary "${BASE_SHA}" -- packages apps scripts \
            > "${EVIDENCE_ROOT}/product.patch"
          test -s "${EVIDENCE_ROOT}/product.patch"
          git reset --hard "${BASE_SHA}"
          git apply --index "${EVIDENCE_ROOT}/product.patch"
          git diff --cached --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          export GIT_AUTHOR_DATE='2026-08-09T00:00:00Z'
          export GIT_COMMITTER_DATE='2026-08-09T00:00:00Z'
          git commit -m "feat(kernel-lite): persist validated Requirement dependencies"
          git rev-parse HEAD > "${EVIDENCE_ROOT}/product-sha.txt"
          git rev-parse HEAD^{tree} > "${EVIDENCE_ROOT}/product-tree.txt"
          git rev-parse "${BASE_SHA}^{tree}" > "${EVIDENCE_ROOT}/base-tree.txt"
          git diff --name-status "${BASE_SHA}..HEAD" | tee "${EVIDENCE_ROOT}/changed-files.txt"
          git status --short > "${EVIDENCE_ROOT}/product-status.txt"
          test ! -s "${EVIDENCE_ROOT}/product-status.txt"
          test "$(cat "${EVIDENCE_ROOT}/product-tree.txt")" != "$(cat "${EVIDENCE_ROOT}/base-tree.txt")"
          test -f packages/templates/src/story-package/requirement-dependency.ts
          test -f packages/templates/tests/requirement-dependency.test.ts
          test -f apps/openovel-runtime/tests/requirement-dependency-production.spec.ts
          grep -q 'predecessorRequirementId' packages/templates/src/story-package/part-one-runtime-types.ts
          grep -q 'successorRequirementId' packages/templates/authoring/sangtian/requirements/part-01.requirements.json
          grep -q 'selectionRules' packages/templates/config/sangtian/story-package/part-one-runtime.json
          test -z "$(git ls-files '.github/repair/requirement-dependency*')"
          test -z "$(git ls-files '.github/workflows/requirement-dependency*')"
      - name: Test product commit
        shell: bash
        run: |
          set -euo pipefail
          results="${EVIDENCE_ROOT}/product/results.tsv"; : > "$results"; failed=0
          run_gate() { local label="$1"; shift; local log="${EVIDENCE_ROOT}/product/logs/${label}.log"; set +e; "$@" 2>&1 | tee "$log"; local code=${PIPESTATUS[0]}; set -e; printf '%s\t%s\t%s\n' "$label" "$code" "$log" >> "$results"; if [ "$code" -ne 0 ]; then failed=1; fi; }
          run_gate 03-story-v4 pnpm test:story:v4
          run_gate 04-templates-typecheck pnpm --filter @ai-story/templates typecheck
          run_gate 05-templates-runtime-contract pnpm --filter @ai-story/templates test:runtime-contract
          run_gate 06-templates-story-package pnpm --filter @ai-story/templates test:story-package
          run_gate 07-templates-build pnpm --filter @ai-story/templates build
          run_gate 08-openovel-typecheck pnpm --filter @apps/openovel-runtime typecheck
          run_gate 09-openovel-test pnpm --filter @apps/openovel-runtime test
          run_gate 10-openovel-build pnpm --filter @apps/openovel-runtime build
          run_gate 11-prisma-generate pnpm db:generate
          run_gate 12-api-solo pnpm --filter @apps/api test:solo-story-engine
          run_gate 13-api-legacy-sangtian pnpm --filter @apps/api test:solo-story-engine:legacy-sangtian
          run_gate 14-branch-persistence pnpm test:story:branch-persistence
          run_gate 15-story-options pnpm test:story:options
          run_gate 16-story-convergence pnpm test:story:convergence
          if [ "$failed" -ne 0 ]; then echo fail > "${EVIDENCE_ROOT}/product/status.txt"; exit 1; fi
          echo pass > "${EVIDENCE_ROOT}/product/status.txt"
      - name: Commit tracked evidence
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json, os, re
          from pathlib import Path
          root=Path(os.environ['EVIDENCE_ROOT']); rows=[]
          pats={k:re.compile(v,re.M) for k,v in {'tests':r'^# tests (\d+)$','pass':r'^# pass (\d+)$','fail':r'^# fail (\d+)$','skip':r'^# skipped (\d+)$','todo':r'^# todo (\d+)$'}.items()}
          for line in (root/'product/results.tsv').read_text().splitlines():
            label,code,log=line.split('\t',2); text=Path(log).read_text(errors='replace'); row={'label':label,'exitCode':int(code),'logPath':log}
            for k,p in pats.items(): m=p.findall(text); row[k]=int(m[-1]) if m else None
            rows.append(row)
          out={'schemaVersion':'requirement-dependency-deterministic-evidence-v1','baseSha':os.environ['BASE_SHA'],'baseTree':(root/'base-tree.txt').read_text().strip(),'testedCodeSha':(root/'product-sha.txt').read_text().strip(),'testedCodeTree':(root/'product-tree.txt').read_text().strip(),'status':'PASS','commands':rows,'supabaseFormal':'NOT_RUN','deepSeek':'NOT_RUN','notes':['All commands ran against testedCodeSha before this evidence file was committed.','The final evidence commit is retested by the same deterministic matrix before push.']}
          target=Path(os.environ['TRACKED_EVIDENCE']); target.parent.mkdir(parents=True,exist_ok=True); target.write_text(json.dumps(out,ensure_ascii=False,indent=2)+'\n')
          PY
          git add "${TRACKED_EVIDENCE}"
          git diff --cached --check
          export GIT_AUTHOR_DATE='2026-08-09T00:01:00Z'
          export GIT_COMMITTER_DATE='2026-08-09T00:01:00Z'
          git commit -m "test(kernel-lite): record deterministic Requirement dependency evidence"
          git rev-parse HEAD > "${EVIDENCE_ROOT}/final-tested-sha.txt"
          git rev-parse HEAD^{tree} > "${EVIDENCE_ROOT}/final-tree.txt"
          git status --short > "${EVIDENCE_ROOT}/final-pretest-status.txt"
          test ! -s "${EVIDENCE_ROOT}/final-pretest-status.txt"
      - name: Retest final exact SHA
        shell: bash
        continue-on-error: true
        run: |
          set -euo pipefail
          results="${EVIDENCE_ROOT}/final/results.tsv"; : > "$results"; failed=0
          run_gate() { local label="$1"; shift; local log="${EVIDENCE_ROOT}/final/logs/${label}.log"; set +e; "$@" 2>&1 | tee "$log"; local code=${PIPESTATUS[0]}; set -e; printf '%s\t%s\t%s\n' "$label" "$code" "$log" >> "$results"; if [ "$code" -ne 0 ]; then failed=1; fi; }
          run_gate 01-story-v4 pnpm test:story:v4
          run_gate 02-templates-typecheck pnpm --filter @ai-story/templates typecheck
          run_gate 03-templates-runtime-contract pnpm --filter @ai-story/templates test:runtime-contract
          run_gate 04-templates-story-package pnpm --filter @ai-story/templates test:story-package
          run_gate 05-templates-build pnpm --filter @ai-story/templates build
          run_gate 06-openovel-typecheck pnpm --filter @apps/openovel-runtime typecheck
          run_gate 07-openovel-test pnpm --filter @apps/openovel-runtime test
          run_gate 08-openovel-build pnpm --filter @apps/openovel-runtime build
          run_gate 09-prisma-generate pnpm db:generate
          run_gate 10-api-solo pnpm --filter @apps/api test:solo-story-engine
          run_gate 11-api-legacy-sangtian pnpm --filter @apps/api test:solo-story-engine:legacy-sangtian
          run_gate 12-branch-persistence pnpm test:story:branch-persistence
          run_gate 13-story-options pnpm test:story:options
          run_gate 14-story-convergence pnpm test:story:convergence
          if [ "$failed" -eq 0 ]; then echo pass > "${EVIDENCE_ROOT}/final/status.txt"; else echo fail > "${EVIDENCE_ROOT}/final/status.txt"; fi
      - name: Write artifact summary
        if: always()
        shell: bash
        run: |
          python - <<'PY'
          import json, os, re
          from pathlib import Path
          root=Path(os.environ['EVIDENCE_ROOT']); rows=[]
          pats={k:re.compile(v,re.M) for k,v in {'tests':r'^# tests (\d+)$','pass':r'^# pass (\d+)$','fail':r'^# fail (\d+)$','skip':r'^# skipped (\d+)$','todo':r'^# todo (\d+)$'}.items()}
          results=root/'final/results.tsv'
          if results.exists():
            for line in results.read_text().splitlines():
              label,code,log=line.split('\t',2); text=Path(log).read_text(errors='replace') if Path(log).exists() else ''; row={'label':label,'exitCode':int(code),'logPath':log}
              for k,p in pats.items(): m=p.findall(text); row[k]=int(m[-1]) if m else None
              rows.append(row)
          out={'inputSha':(root/'input-sha.txt').read_text().strip() if (root/'input-sha.txt').exists() else None,'baseSha':os.environ['BASE_SHA'],'baseTree':(root/'base-tree.txt').read_text().strip() if (root/'base-tree.txt').exists() else None,'testedCodeSha':(root/'product-sha.txt').read_text().strip() if (root/'product-sha.txt').exists() else None,'testedCodeTree':(root/'product-tree.txt').read_text().strip() if (root/'product-tree.txt').exists() else None,'finalTestedSha':(root/'final-tested-sha.txt').read_text().strip() if (root/'final-tested-sha.txt').exists() else None,'finalTree':(root/'final-tree.txt').read_text().strip() if (root/'final-tree.txt').exists() else None,'status':(root/'final/status.txt').read_text().strip() if (root/'final/status.txt').exists() else 'missing','commands':rows,'supabaseFormal':'NOT_RUN','deepSeek':'NOT_RUN'}
          (root/'summary.json').write_text(json.dumps(out,ensure_ascii=False,indent=2)+'\n')
          PY
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: requirement-dependency-evidenced-${{ github.sha }}
          path: /tmp/requirement-dependency-evidenced/
          if-no-files-found: error
          retention-days: 7
      - name: Replace feature branch with exact green final SHA
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          test "$(cat "${EVIDENCE_ROOT}/product/status.txt")" = pass
          test "$(cat "${EVIDENCE_ROOT}/final/status.txt")" = pass
          test "$(git rev-parse HEAD)" = "$(cat "${EVIDENCE_ROOT}/final-tested-sha.txt")"
          input_sha="$(cat "${EVIDENCE_ROOT}/input-sha.txt")"
          git push \
            --force-with-lease="refs/heads/${TARGET_BRANCH}:${input_sha}" \
            origin "HEAD:refs/heads/${TARGET_BRANCH}"
          git rev-parse HEAD > "${EVIDENCE_ROOT}/final-remote-sha.txt"
