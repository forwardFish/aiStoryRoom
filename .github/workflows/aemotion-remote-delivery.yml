name: A-Emotion M1 delivery carrier (dormant)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dormant:
    runs-on: ubuntu-latest
    steps:
      - run: echo "A-Emotion M1 delivery carrier is dormant"
