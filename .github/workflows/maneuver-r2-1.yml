name: Maneuver R2-1 OpenNovel projection

on:
  push:
    branches:
      - feat/mvp-four-maneuver-actions

permissions:
  contents: read

concurrency:
  group: maneuver-r2-1-${{ github.ref }}
  cancel-in-progress: true

jobs:
  neutralized:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "R2-1 execution moved to the registered Causal MVP workflow"
