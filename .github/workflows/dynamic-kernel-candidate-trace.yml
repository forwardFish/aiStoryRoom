name: Dynamic Kernel Structural Candidate Trace

# Re-run after adding structured Pending Rule routing evidence.
on:
  push:
    branches:
      - codex/chatgpt-pro-dynamic-kernel-lite
    paths:
      - .github/workflows/dynamic-kernel-candidate-trace.yml
  workflow_dispatch:

permissions:
  contents: read
  statuses: write

jobs:
  trace:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      EVIDENCE_ROOT: /tmp/dynamic-kernel-candidate-trace
    steps:
      - name: Publish trace status
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api --method POST \
            "repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
            -f state=pending \
            -f context="dynamic-kernel-lite/candidate-trace" \
            -f description="Structural candidate traces running" \
            -f target_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 10.12.1
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Apply current repair layers
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "${EVIDENCE_ROOT}"
          tr -d '[:space:]' < .github/repair/dynamic-kernel-followup.patch.gz.b64 \
            | base64 --decode | gzip --decompress > "${RUNNER_TEMP}/followup.patch"
          git apply --check "${RUNNER_TEMP}/followup.patch"
          git apply "${RUNNER_TEMP}/followup.patch"
          python .github/repair/apply-dynamic-kernel-final-repair.py
          python .github/repair/apply-dynamic-kernel-continuity-repair.py
          python .github/repair/apply-dynamic-kernel-sequence-compat-repair.py
          pnpm install --frozen-lockfile
      - name: Capture three-turn candidate trace
        shell: bash
        run: |
          set -euo pipefail
          pnpm exec tsx .github/repair/trace-dynamic-kernel-three-turns.ts \
            | tee "${EVIDENCE_ROOT}/three-turn-candidate-trace.json"
      - name: Upload trace evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dynamic-kernel-candidate-trace-${{ github.sha }}
          path: /tmp/dynamic-kernel-candidate-trace/
          if-no-files-found: error
          retention-days: 3
      - name: Publish trace completion
        if: always()
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api --method POST \
            "repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
            -f state=success \
            -f context="dynamic-kernel-lite/candidate-trace" \
            -f description="Structural candidate traces uploaded" \
            -f target_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
